# macOS / editor hygiene
.DS_Store
.claude/settings.local.json

# Secrets / local config
.mcp.json
.env
docker/.env
docker/volumes/

# Build output
node_modules/
dist/
__pycache__/
*.pyc

# Tauri / native-app build artifacts (sidecar bundle ~136 MB, Rust target cache, auto-gen)
app/src-tauri/sidecar-payload/
app/src-tauri/target/
app/src-tauri/gen/

# Astro landing-site build (lives in its own repo / build target — never tracked here)
site/

# Logs + ephemeral
*.log
*.sql.bak
*.backup
logs/

# User-specific narrative / non-shippable docs
SYSTEM_OVERVIEW.md
SETUP_ANLEITUNG.md
