{
  "name": "mycelium-mcp-server",
  "version": "0.1.0",
  "description": "mycelium — cognitive architecture MCP server (Supabase pgvector + Ollama). Works with any MCP client.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run clean && tsc && node --test \"dist/__tests__/**/*.test.js\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@supabase/supabase-js": "^2.49.4",
    "ollama": "^0.5.16",
    "zod": "^3.25.1"
  },
  "optionalDependencies": {
    "node-llama-cpp": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
